###

Overrides the default lineman files settings.

To see what the defaults are:
- try running `lineman config files`
- or look in node_modules/lineman/config/files.coffee

###

module.exports = (lineman) ->
  return {}
